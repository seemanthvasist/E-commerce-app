apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommerce-deployment
  namespace: vasist-ns

spec:
  replicas: 3
  selector:
    matchLabels:
      app: ecommerce
  template:
    metadata:
      labels:
        app: ecommerce
    spec:
      containers: 
      - name: vegitable-app
        image: vasist/e-commerce-app:v1.0.0
        ports:
        - containerPort: 80
